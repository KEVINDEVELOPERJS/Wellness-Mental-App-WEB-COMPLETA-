# Disable npm scripts that cause warnings in Vercel
ignore-scripts=false
# Opt out of npm audit warnings during install
audit=false
# Disable fund messages
fund=false
# Set engine strict to avoid version issues
engine-strict=true